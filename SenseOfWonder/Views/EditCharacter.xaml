<UserControl x:Class="SenseOfWonder.Views.EditCharacter"
				 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
				 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
				 xmlns:SenseOfWonder="clr-namespace:SenseOfWonder"
				 xmlns:Model="clr-namespace:SenseOfWonder.Model"
				 mc:Ignorable="d" 
				 d:DesignHeight="300" d:DesignWidth="300">
	<Grid DataContext="{Binding Character}" d:DataContext="{d:DesignInstance {x:Type Model:WonderCharacterDesignData}, IsDesignTimeCreatable=True}">
		<TextBox x:Name="CharName" Height="23.2" Margin="46.297,8,8,0" TextWrapping="Wrap" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top"/>
		<TextBlock HorizontalAlignment="Left" Margin="8,11,0,0" TextWrapping="Wrap" Text="Name:" VerticalAlignment="Top"/>
		
	 </Grid>
</UserControl>
